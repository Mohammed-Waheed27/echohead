import 'package:flutter/material.dart';

/// Widget for custom trash can marker (can be extended later)
class TrashCanMarker extends StatelessWidget {
  const TrashCanMarker({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}

